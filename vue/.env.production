# Leave empty when production uses a same-origin reverse proxy for /api and /uploads.
# Set to the backend origin, for example https://api.example.com, when deploying separately.
VITE_API_BASE=

# Keep production wired to real statistics APIs.
VITE_ADMIN_DEMO_DATA=false
