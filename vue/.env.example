# Backend origin used by the shared Axios client.
# Do not include /api because API modules already use /api paths.
VITE_API_BASE=http://localhost:8089

# Enable local demo data for admin statistics charts when backend data is empty.
VITE_ADMIN_DEMO_DATA=false
